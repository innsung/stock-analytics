# V3.2.1 Phase 6.27 — Release artifact integrity and handoff

This independent verifier re-hashes every Phase 6.26 manifest target, validates exact sizes, rechecks the 14/14 release-gate summary, opens the release ZIP, tests its CRC, and requires the canonical ledger, gate audit, gate summary, and SHA-256 manifest inside the archive.

Operational handoff: use the Phase 6.26 ZIP as the canonical release bundle. The three unresolved rows are intentionally terminal-routed: two non-PIT dividends are deferred and one complex split remains externally blocked. No actionable event-resolution work remains.
