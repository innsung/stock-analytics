# V3.2.1 Phase 6.30 — Release candidate seal

`V3.2.1-RC1` is an artifact-level release candidate, not a Git tag. The seal records SHA-256 and size for the canonical ledger, Phase 6.26 release ZIP, requirements, and dependency lock, together with PASS attestations from quality, integrity, restore, and runtime gates.

Operational status: 399 ledger rows are fully accounted for; actionable work is zero; two non-PIT dividend rows remain intentionally deferred and one complex split row remains externally blocked. A formal Git commit/tag should be created only after the existing dirty worktree is reviewed and intentionally curated.
